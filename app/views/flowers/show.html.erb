<div class="border-top">
    <div class="row justify-content-center">
        <div class="col-6">
            <h1>Flower#show</h1>
                  <p>
                    <%= @flower.name %>
                    <%= @flower.species %>
                  </p>
                <%= link_to 'Back', flowers_path  %>
        </div>
    </div>
</div>
